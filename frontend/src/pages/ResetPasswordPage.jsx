import { useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { Check, CheckCircle2, Eye, EyeOff, LoaderCircle, Lock, Wallet } from "lucide-react";
import toast from "react-hot-toast";
import api from "../services/api";

const ResetPasswordPage = () => {
  const { token } = useParams(); const navigate = useNavigate(); const [password, setPassword] = useState(""); const [confirm, setConfirm] = useState(""); const [show, setShow] = useState(false); const [loading, setLoading] = useState(false); const [success, setSuccess] = useState(false); const [error, setError] = useState("");
  const rules = [password.length >= 6, /[A-Z]/.test(password), /\d/.test(password)]; const strength = rules.filter(Boolean).length;
  const submit = async (event) => { event.preventDefault(); if (loading) return; if (password.length < 6) return setError("Use at least 6 characters."); if (password !== confirm) return setError("Passwords do not match."); setLoading(true); setError(""); try { const { data } = await api.post(`/auth/reset-password/${token}`, { password }); setSuccess(true); toast.success(data.message); setTimeout(() => navigate("/login"), 2200); } catch (requestError) { const message = requestError?.response?.data?.message || "Unable to reset password."; setError(message); toast.error(message); } finally { setLoading(false); } };
  return <main className="min-h-screen bg-[#0B1120] px-5 py-10 text-white"><div className="mx-auto flex min-h-[80vh] max-w-md items-center"><section className="w-full rounded-3xl border border-slate-700/70 bg-[#131A2A]/90 p-7 shadow-2xl shadow-black/20 backdrop-blur-xl sm:p-9"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-gradient-to-br from-cyan-400 to-blue-600"><Wallet size={23} /></span><div><p className="font-semibold">FinanceFlow</p><p className="text-sm text-slate-400">Secure password reset</p></div></div>{success ? <div className="mt-10 text-center"><CheckCircle2 className="mx-auto text-emerald-300" size={48} /><h1 className="mt-5 text-2xl font-bold">Password updated</h1><p className="mt-3 text-sm leading-6 text-slate-400">You can now sign in with your new password. Redirecting you securely…</p></div> : <><h1 className="mt-9 text-3xl font-bold">Choose a new password</h1><p className="mt-3 text-sm leading-6 text-slate-400">Use a unique password you don’t use elsewhere.</p><form onSubmit={submit} className="mt-7 space-y-5"><label className="block text-sm font-medium text-slate-300">New password<div className="relative mt-2"><Lock size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-500" /><input type={show ? "text" : "password"} autoComplete="new-password" value={password} onChange={(event) => setPassword(event.target.value)} className="w-full rounded-2xl border border-slate-700 bg-slate-900/60 py-3 pl-11 pr-12 text-white outline-none focus:border-cyan-400" /><button type="button" aria-label={show ? "Hide password" : "Show password"} onClick={() => setShow(!show)} className="absolute right-4 top-1/2 -translate-y-1/2 text-slate-400">{show ? <EyeOff size={18} /> : <Eye size={18} />}</button></div></label><div className="h-1.5 overflow-hidden rounded-full bg-slate-800"><div className={`h-full rounded-full transition-all ${strength === 3 ? "w-full bg-emerald-400" : strength === 2 ? "w-2/3 bg-amber-400" : "w-1/3 bg-rose-400"}`} /></div><ul className="grid gap-1 text-xs text-slate-500">{["At least 6 characters", "One uppercase letter", "One number"].map((label, index) => <li key={label} className={rules[index] ? "text-emerald-300" : ""}><Check size={13} className="mr-1 inline" />{label}</li>)}</ul><label className="block text-sm font-medium text-slate-300">Confirm password<input type={show ? "text" : "password"} autoComplete="new-password" value={confirm} onChange={(event) => setConfirm(event.target.value)} className="mt-2 w-full rounded-2xl border border-slate-700 bg-slate-900/60 px-4 py-3 text-white outline-none focus:border-cyan-400" /></label>{error && <p className="text-xs text-rose-300">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-2xl bg-cyan-400 py-3 font-semibold text-slate-950 disabled:opacity-60">{loading ? <><LoaderCircle size={17} className="animate-spin" /> Updating…</> : "Update password"}</button></form><Link to="/login" className="mt-5 block text-center text-sm text-slate-400 hover:text-cyan-300">Back to sign in</Link></>}</section></div></main>;
};
export default ResetPasswordPage;
